/* IIFE, Immediately Invoked FFunction Expression is used to not interruption with the global variables */

// this can be implemented in every type of function 

(function greet(){
    var hello = "hi";
    console.log(hello);
})();
